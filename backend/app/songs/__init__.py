"""Tunora's durable creative domain: Song -> Version -> Audio.

A Song is the creative identity, a Version is one immutable generation
snapshot of it, and audio belongs to a Version. A Job (app.jobs) is only the
execution of one Version's generation.
"""
